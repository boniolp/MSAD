Here the predictions produced from the eval_* functions are saved, or when we train with the parameter --eval-true.

Files here are overwritten, this means that the results from two runs of the same model will be overwrite each other. Be careful
